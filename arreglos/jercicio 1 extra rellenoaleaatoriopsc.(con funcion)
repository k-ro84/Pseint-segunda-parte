SubProceso llenado(vector1,num)
	Definir i Como Entero
	Para i<-0 Hasta num-1 Hacer
		vector1(i)=Azar(11)
	Fin Para
FinSubProceso
//1 extra. Realizar un programa que rellene dos vectores al mismo tiempo, con 5 valores aleatorios
//y los muestre por pantalla.
Algoritmo relleno_aleat
	Definir num,i,vector,vector1,vector2 Como Entero
	i<-0
	num<-0
	
	Escribir "ingrese el tamaño de sus vectores"
	Leer num;
	Dimension vector1(num), vector2(num)
	
	llenado(vector1,num)
	llenado(vector2,num)
	Para i<-0 Hasta num-1 Hacer
		Escribir "posicion", i , "(", vector1(i), ")"Sin Saltar
	Fin Para
	Escribir ""
	Para i<-0 Hasta num-1 Hacer
		Escribir "posicion", i , "(", vector2(i), ")" Sin Saltar
	Fin Para
	Escribir""
	
FinAlgoritmo
